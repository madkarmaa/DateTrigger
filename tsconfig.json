{
    "compilerOptions": {
        "target": "es6", // or es6 if you don't need to support older browsers
        "module": "commonjs", // use commonjs for Node.js
        "outDir": "dist", // output directory for compiled files
        "declaration": true, // generate .d.ts files for type information
        "strict": true, // enable strict type checking
        "esModuleInterop": true, // allow interoperability between CommonJS and ES modules
        "lib": ["es2020", "dom"] // include necessary libraries (e.g., es2020 for modern JavaScript features and dom for browser APIs)
    },
    "include": ["src/**/*"], // include all files in the src directory
    "exclude": ["node_modules"] // exclude node_modules directory
}
